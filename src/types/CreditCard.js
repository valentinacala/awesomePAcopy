/**
 * AWESOME PA
 * A React Native App
 *
 * @flow
 */

export type CreditCard = {
    id: number,
    brand: string,
    lastUsage: string,
    number: string,
    image: any // eslint-disable-line flowtype/no-weak-types
};

